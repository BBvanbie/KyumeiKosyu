# 予約確定状態と確認キー再発行設計

## 目的

電話確定の実運用に合わせて、予約の `確定日時` を記録・表示できるようにする。あわせて、管理者が確認キーを閲覧せずに再発行できるようにする。

## 方針

- `Reservation` に `confirmedAt` を追加する
- 管理者が `確定` を押した時に `confirmedAt` を保存する
- 利用者向け予約確認画面では状態を日本語表示し、確定日時を見せる
- 管理者は確認キーを見ない
- 管理者は確認キーを再発行できる
- 再発行時は古い確認キーを無効化し、新しい確認キーを一度だけ表示する
